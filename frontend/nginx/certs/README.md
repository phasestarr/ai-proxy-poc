Place real TLS material here when you want to replace the container's
fallback self-signed certificate.

Expected filenames:
- `tls.crt`
- `tls.key`

The frontend NGINX runtime looks for these files at `/etc/nginx/certs/`.
